.tentacleView--query {
  position: relative;
  width: 40px;
  height: 30px;
  line-height: 30px;
  background #fff;
  margin 0 5px;
  text-align center;
  border-radius 5px;
  font-size: 14px;
}

.for--fixed {
  position: fixed;
  left: 0;
  right: 0;
  top: 0;
}

.search--ent--list {
  padding-top: 50px;
}
